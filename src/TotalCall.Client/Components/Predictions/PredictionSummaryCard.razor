<AppCard Padding="Sm" AdditionalClass="@AdditionalClass">
    <div class="prediction-summary-card">
        <div class="prediction-summary-card__header">
            <p class="prediction-summary-card__label">@Label</p>
            <p class="prediction-summary-card__value">@Value</p>
        </div>
        @if (ProgressPercent is not null)
        {
            <div class="progress-track" aria-hidden="true">
                <span style="@($"width: {Math.Clamp(ProgressPercent.Value, 0, 100)}%")"></span>
            </div>
        }
        @if (!string.IsNullOrWhiteSpace(Description))
        {
            <p class="prediction-summary-card__description">@Description</p>
        }
    </div>
</AppCard>

@code {
    [Parameter, EditorRequired]
    public string Label { get; set; } = string.Empty;

    [Parameter, EditorRequired]
    public string Value { get; set; } = string.Empty;

    [Parameter]
    public int? ProgressPercent { get; set; }

    [Parameter]
    public string? Description { get; set; }

    [Parameter]
    public string? AdditionalClass { get; set; }
}
